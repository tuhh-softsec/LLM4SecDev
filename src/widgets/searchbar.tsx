const Searchbar = () => {
    return (
        <div className="my-4 rounded border-2">
            <input className="w-full p-1" placeholder="Search for a model (seperate multiple queries with `;`) and press enter"/>
        </div>
    );
};

export default Searchbar;